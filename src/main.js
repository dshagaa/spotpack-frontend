// src/main.js — SpotPack entry point
import './style.css';

// Alpine components are registered inline in index.html via <script type="module">
// Routing is handled by x-data on <body> with hashchange listener
